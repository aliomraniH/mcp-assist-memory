modules = ["python-3.11"]
run = "python main.py"
entrypoint = "main.py"

[deployment]
run = ["python", "main.py"]
# Reserved VM (not Autoscale): Phase 0's durability gate needs the process/disk
# to persist, and the Postgres backend keeps one long-lived connection pool.
deploymentTarget = "vm"
