# evlog project notes

Project memory lives in `.agents/memory/` — read `MEMORY.md` (the index)
before making structural changes, and record durable lessons there when a
session ends.

Tests: `python -m pytest -q` from the project root.
